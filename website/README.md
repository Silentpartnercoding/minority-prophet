# Public interface

The public interface is implemented in `app/`. It combines the mission, benchmark explanation, baseline leaderboard, and a synthetic evidence dashboard. Values shown in the first release are explicitly labeled as a demonstration world until a frozen benchmark result is published.
