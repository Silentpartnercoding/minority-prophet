# Wikipedia citogenesis — labels

Each case requires a frozen adjudication packet identifying the originating
revision, external pickup, and later citation back into Wikipedia. The packet
must cite timestamped primary records. A case with an incomplete loop is
`unknown`, not a negative. The experiment tests whether the frozen loop can be
recovered without exposing the adjudication fields to the method.

