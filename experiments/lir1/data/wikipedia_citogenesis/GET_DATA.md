# Wikipedia citogenesis — acquisition

Freeze an incident list and retrieve page revisions through the documented
MediaWiki revisions API:
`https://www.mediawiki.org/wiki/API:Revisions`.
Retrieve archived external pages only through permitted Internet Archive
interfaces. Store revision IDs, timestamps, SHA-1 values, archive timestamps,
and request URLs. Missing archived content remains missing.

