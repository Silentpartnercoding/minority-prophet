# MemeTracker — labels

Phrase-cluster membership and timestamps are observed corpus fields. Exact
parent-child mutation edges are not supplied ground truth and remain predicted.
Evaluation may test held-out hyperlinks where present and root-family agreement
against cluster membership, but it must not report inferred mutation trees as
true trees. Shared rare mutations are features, not labels.

