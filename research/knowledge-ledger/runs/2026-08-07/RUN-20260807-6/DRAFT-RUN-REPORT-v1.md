# DRAFT RUN REPORT v1 — RUN-20260807-6

Two bounded tasks on owner direction, after which **the program is closed
again, and KL-001 carries no committed gate** — its real first gate is named
and costed, committing it is the owner's act.

## Task 1 — KL-011's stale blocker, corrected as a correction

The blocker "KL-000 independent reimplementation" was discharged by
IND-20260807-1/-2/-3 — commissioned once, exactly as the record's superseded
text said it should be — and the record did not know it. KL-011's STATUS now
cites the discharging evidence (with the LEAK-101 qualification travelling
along), preserves the superseded text verbatim, keeps the state at `seeded`,
and states what genuinely remains: its v0.2 preregistration, the five-stage
pipeline, its fixtures — plus the inheritances from KL-000's close (A2
undecided at 19,152 worlds, SCH-005's missing protected fields, ADV-001,
F11). Recorded as the latest instance of the stale-self-description family
(PROV-005, ART-101, TEST-101, the DOC-102 chain): records expire while the
world moves, and only re-derivation catches it.

## Task 2 — KL-001's named first check: verdict (a), said plainly

The check ran (preregistered first, twice — see the defect below) against
the unchanged evaluator. The incomplete-coverage repository **cannot obtain
a clean conclusion** (E1), the complete variant can (E2), and the check is
**KL-000's I2 restated**: the same structure re-labelled into KL-000's
vocabulary is one of the already-enumerated 176,120 worlds, and its receipt
is identical in every name-free field — the divergence is exactly the echoed
identifier strings and the digest they induce (FC1.1, E4′/E5′).
"Mandatory file" maps to "declared location" by pure renaming. **No new
evidence about the evaluator was produced; KL-001's named first gate was
already paid by KL-000.** That answer cost four synthetic worlds instead of
a corpus, which is the cheap kind of result.

Demonstrated alongside, on purpose (W3): the same repository reaches a
*clean* conclusion by omitting the uncovered files from its declaration —
ADV-001 in repository costume. The layer that decides which files are
mandatory is where KL-001's real risk lives, and it does not exist.

**The real first gate, named and costed** (KL-001 STATUS, not committed):
the mapping pipeline with registered declaration rules (200–400 lines), the
seeded, digest-manifested planted-defect corpus (300–500 lines), the
false-clean and pre-ledger recall endpoints, and the v0.2 preregistration —
roughly one focused run; zero spend unless a metered model enters the
pipeline, which needs founder authorization first.

## A registration defect of this run, self-caught by execution

FC1's E4 **failed as registered**: it demanded W1/W4 identity on "all of
`evidence`", but `supportingRoots`/`opposingRoots` echo declared rootId
strings by the registered receipt object — the expectation quantified over
name-carrying fields. The failed result is preserved unmodified; FC1.1
corrected the expectation by new registration and confirmed verdict (a).
The error class is the program's M15/H1 family — a concept written down
instead of the fields it quantifies over — now with its own methodology note
(M24).

## What did not move

KL-000 `adversarial-passed` at v1.3.0, untouched; 74 + 88 tests pass.
**Draft PR #17 verified open at exactly `650f9ee`** (the head this program
built, pushed unmodified by the owner) — untouched by this run. Ten kernels
unchanged; KL-001 and KL-011 gained corrected records, no state changes.
Environment observation: a new remote branch `codex/exp009-confirmatory`
appeared on fetch — someone else's work, recorded and untouched.

## Commits

| SHA | What |
|---|---|
| `0e09da7` | run open: reopening recorded; PR #17 verified read-only |
| `b9a30ef` | KL-011 correction: blocker discharged, superseded text preserved |
| `1bcf474` | **FC1 registration** (before execution) |
| `d418e7b` | FC1 executed: E1–E3 pass, E4 fails as registered (drafting error preserved) |
| `d76055b` | **FC1.1 registration** (corrected E4, before execution) |
| `d267e9d` | FC1.1 verdict (a); FINDING-FC1; KL-001 record corrected, stays seeded |
| *(packet)* | this closing packet |

The first-transaction gate remains **NOT REACHED**. Eleven kernels remain
seeded. Nothing is promoted.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
