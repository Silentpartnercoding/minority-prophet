# NEXT-RUN-PROPOSAL v1 — after RUN-20260807-8

No committed gate exists. The queue is BL-039 (owner scheduling): eleven
kernels are schema-ready, and each one's preregistration now states what its
registration must supply, as reasons rather than nulls. The two with the
most groundwork are KL-001's real first gate (mapping pipeline + frozen
corpus + endpoints, with BL-031/BL-032 inside it) and KL-011's v0.2
registration — the latter gated in substance by the A2 decision (19,152
worlds), which is now the program's last undecided conclusion-function
question. Structurally in front of KL-006/KL-008 only: BL-037, the
receipt-schema dependency extension — the largest and most deliberately
versioned change the program could make, never to be mixed into a
nothing-moves registration.

Any next brief that requires a packet cites
tests/test_closing_packets.py::REQUIRED (BRF-101; re-typed enumerations
drift — this run's brief proved it). Any next run's verification gates on
exit status (M27/BL-038).
