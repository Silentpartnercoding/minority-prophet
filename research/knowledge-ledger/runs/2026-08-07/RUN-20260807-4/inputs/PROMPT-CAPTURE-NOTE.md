# Prompt capture provenance — RUN-20260807-4

`PROMPT.txt` is an **agent transcription** from the run context — the fourth
consecutive run with no operator-side prompt artifact (PROV-004/008/009). The
digest in `run-manifest.json` authenticates the transcription only and is
unverified provenance. No scientific number derives from the prompt text;
every claim this run records was verified against committed artifacts or the
imported independent results before being acted on.
