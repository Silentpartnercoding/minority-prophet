# NEXT-RUN-PROPOSAL v1 — after RUN-20260807-4

There is no next run of this program; this file records what the next run
**would** be, so that the committed gate cannot lapse by the program having
ended (the reason I12 was made a gate rather than a candidate applies to the
program's own closure most of all).

## If and when the owner schedules it: v1.3.0

One registration, one re-run, same disciplines:

1. **I12** (the committed gate): `conclusion == conclusionFunction(world)`
   and `margin == abs(|S|−|O|)` as a hard invariant. Pass condition already
   registered: the two inversion ablations are caught on exactly 22,440 and
   38,760 worlds with no fixture involved; B5 records zero I12 violations;
   no other number moves.
2. The owner's A2 decision (19,152 worlds), if reached — and A1 (4 worlds)
   with it; both then enter `conclusionFunction` as decided text under I12's
   enforcement, with the rejected readings preserved as R1's was.
3. The digest-moving bundle, together or not at all: margin rename or
   `majoritySide` member (IND-3 §3), `conversionsToReverse` at the empty
   ledger (F4). Every digest re-pins; C11/C12 get new expected values; the
   change is versioned deliberately.
4. The standing textual repairs: world-referential I2/I5 (F1/F2), B2/B3
   dedup plus the B6 ablation, F13's fixture note, NAM-101's vocabulary,
   F12's adversarial-phase specification, F11's stream registration or a
   permanent replication-only declaration.

## If a fourth implementation is ever commissioned

The package: derived by deletion, manifested, screened per file in both
number formats, **conclusion distribution withheld** (LEAK-102/H3), paths
resolving as registered — and run on **a machine that does not hold the
reference**, the one condition never met in three IND runs. Its agreement on
the pinned digests would then be evidence of the strongest kind this program
has defined.

## What comes after that, unchanged since RUN-1

KL-011 preregistration at schema v0.2 (its two-implementation prerequisite
is substantively met; its schema, protected-field, and under-declared-scope
blockers stand), then the five-stage transaction with the registered
injection set, then a third-environment reproduction — and only after all of
that, a Candidate First Transmission becomes nameable. Neither title exists
today. The first-transaction gate is NOT REACHED.
