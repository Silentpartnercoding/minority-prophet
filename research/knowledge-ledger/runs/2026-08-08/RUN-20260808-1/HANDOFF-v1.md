# Handoff — after RUN-20260808-1

## State

Nothing registered, no kernel advanced. Three records written (XRP-101, the
profile's placement table, BL-046) and one commission packaged.

## The commission is ready to hand out

`$HOME/Development/lin000-spec` — BRIEF.md, REGISTRATION.md, RESEARCH-METHOD.md,
MANIFEST.sha256. Screened clean against all fourteen withheld counters in both
spellings, including the brief itself, which an earlier draft failed.

Give it to a fresh implementer in a language other than Python. The pass
condition is the two stream digests in the brief, not the counters.

## What the next run must know

**Publication is a leak surface (M27).** Before publishing any run record, screen
it against the withheld set of every experiment it proposes or leaves open. This
run found twelve counters already public because the proposal and the numbers
that falsify it shipped in the same commit.

**BL-046 before more product claims.** Three shared quantities remain unmeasured
across implementations, and the one comparable audit found one of three diverging
in a way that mispriced a published security claim by 2x.

**BL-042 stays urgent** — TRC-101 applies at registration time, and the next
kernel registration is the moment it either holds or is skipped.

## Owner queue, unchanged by this run

A2 (BL-043), paper v1.0.4 adoption and PPR-101 (BL-045), the digest-moving bundle
(BL-026/BL-037), registration scheduling (BL-039).
