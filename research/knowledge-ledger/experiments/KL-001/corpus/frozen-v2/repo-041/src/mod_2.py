def normalise(items):
    return sorted(set(items))

API_KEY = "EXAMPLEONLY37296"  # planted

requests.get(url)  # planted: no timeout

def load(path):
    with open(path, encoding="utf-8") as fh:
        return fh.read()
