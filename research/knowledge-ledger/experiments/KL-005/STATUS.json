{
  "schema": "minority-prophet.experiment-status.v0.1",
  "experimentId": "KL-005",
  "realm": "journalism",
  "state": "seeded",
  "resultStatus": "none",
  "lastCompletedGate": "registry-entry",
  "reviewedByRun": "RUN-20260915-1",
  "nextGate": "Write the registration against the now-existing two-sided metric, then assemble timestamped closed news events with ground truth and preregister BOTH false early confirmation and delay to correct confirmation. The metric was the prerequisite and is no longer the blocker; the corpus is.",
  "blockers": [
    "DISCHARGED 2026-09-15: 'directory lacks fixtures/, src/, tests/, results/, REPRODUCE.md (5 of 8 required entries); see run constraint ENG-001' -- fixtures/, src/, tests/ and REPRODUCE.md are present. results/ is deliberately absent while seeded; probe output is in probe/.",
    "MET IN SUBSTANCE 2026-09-15, pending registration: 'a single-endpoint design would let indefinite abstention win; the two-sided metric is the prerequisite, not a refinement'. The metric exists in src/metric.py and the defect it repairs is demonstrated. It is not discharged until a registration cites it.",
    "OPEN and not solvable within a fixture: a real run needs timestamped closed news events with ground truth."
  ],
  "claimAllowed": "Nothing. This experiment is seeded and supports no result claim. The unregistered probe establishes only that the declared root-collapse rule resolves wire copies and citation cycles to their true origin count on a structural fixture, and that the declared two-sided metric ranks a never-confirming system last where the one-sided endpoint ranks it first. Not a rate, not a news corpus, nothing about journalism.",
  "dependsOn": [
    "KL-000"
  ],
  "dependsOnNote": "EXPERIMENT-REGISTRY.json states one KL-000 violation blocks every downstream kernel. KL-000 reached adversarial-passed in RUN-20260807-1, so this dependency is satisfied for fixture-stage work but NOT for any promotion: KL-000 itself awaits independent reimplementation.",
  "schemaMigration": "preregistration.json migrated v0.1 -> v0.2 by RUN-20260807-8 (0 schema blocker(s) discharged). State unchanged: seeded. A schema with more fields is not evidence.",
  "unregisteredProbe": {
    "ranAt": "2026-09-15",
    "isAResult": false,
    "why": "Same as KL-002: run before any registration exists, so it informs one rather than reporting against one. Output is in probe/, not results/.",
    "checks": {
      "syndicationCannotManufactureIndependence": "HOLDS -- 5 wire reports resolve to 1 origin; 7 syndicated to 1; 3 genuine originals stay 3; a 3-outlet citation cycle resolves to 0, fail-closed.",
      "twoSidedMetricDeniesSilenceTheWin": "HOLDS -- the never-confirming system scores 0.0 (best) on the one-sided endpoint and 1.0 (worst) on the two-sided one; root_aware wins the two-sided at 0.533."
    },
    "bearing": "The prerequisite the protocol names -- 'a single-endpoint design would let indefinite abstention win; the two-sided metric is the prerequisite, not a refinement' -- now exists as code, with the defect it repairs demonstrated rather than asserted."
  }
}
