# LIR-1 PHEME acquisition inventory

This record proves acquisition and normalization shape only. It contains no
lineage-inference score and no confirmatory result.

The official 46,529,729-byte PHEME archive matched Figshare's supplied MD5 and
was normalized under the 5,000-claim pilot cap. The raw archive, extracted
tweets, and normalized JSONL remain in ignored local `artifacts/`; the tracked
inventory exposes source hashes, counts, exclusions, and the normalized-file
hash without redistributing tweet text.

See `experiments/lir1/PHEME-PILOT-PROTOCOL.md` for the frozen next step.
