# Corrected PHEME development threshold

This development-only record evaluates the 398 edges actually hidden at the
40% perturbation. The frozen rule selected threshold **0.40** with hidden-parent
F1 `0.1140529532` (precision `0.3010752688`, recall `0.0703517588`).

This is materially weaker than the void all-edge score. At the same threshold,
root-pair F1 was `0.3753874624` and mean absolute root-count error was
`4.3571428571` per case. The transparent text-and-time baseline is therefore a
weak recovery method on PHEME development data. No confirmatory claim is made.

